// Junio 2016

// Exponencial
const data = 3 ** 4
console.log(data)

// Array includes
let numbers = [1, 2, 3, 4] // Para objetos se usa in, hasOwnProperty o Object.hasOwn(objeto, dato)
const list = ['Emi', 'Yessi', 'Mateo']
console.log(numbers.includes(4))
console.log(list.includes('emi'))